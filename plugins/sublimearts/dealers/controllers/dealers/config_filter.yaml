# ===================================
# Filter Scope Definitions
# ===================================

scopes:

    groups:

        # Filter name
        label: Filtered by Group(s)

        # Model Class name
        modelClass: SublimeArts\Dealers\Models\DealerGroup

        # Model attribute to display for the name
        nameFrom: name

        # Filter scope
        scope: filterByGroup
